# Pinned to exact versions so a CI run six months from now installs what was
# tested today. `>=` floors on a fast-moving stack (FastAPI, Pydantic) turn an
# unrelated upstream release into a red build with no local reproduction.
#
# To upgrade: bump deliberately, run `pytest`, commit the new pin.

fastapi==0.141.1
uvicorn[standard]==0.52.1
sqlalchemy==2.0.51
pydantic==2.13.4
# Settings validated at start-up, so a missing GATEWAY_API_KEY fails loudly
# rather than silently disabling authentication.
pydantic-settings==2.15.0
