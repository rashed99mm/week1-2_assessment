# Load the topology from definitions.json at boot.
#
# Declaring exchanges and queues here rather than in each service means no
# service can take another down by declaring the same object with different
# arguments — a PRECONDITION_FAILED on deploy that only reproduces when two
# versions are running at once.
load_definitions = /etc/rabbitmq/definitions.json
